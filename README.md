# Demo code for the React Performance coder talk

* `v1`: contains the code without memoization
* `v2`: contains the code with memoization
* `v3`: contains the code with asynchronous execution and web workers
